let arr = ['js', 'css', 'python', 'c++', 'cshare', 'java', 'html', 'klark', 'port', 'no']

let i = 0 
while(i < arr.length) {
    if (arr[i].length > 3) {
        console.log(arr[i])
    }
    i++
}