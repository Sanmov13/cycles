let arr = ['john', 'Anthony', 'Andrew', 'Andy', 'Boris', 'Ben', 'Paul', 'Pier', 'Ada', 'Man']

for (let i = 0; i < arr.length; i++) {
    if (arr[i].startsWith('A')) {
        console.log(arr[i])
    }
}